{
    "_comment": [
        "Step in M, S and U with a timer interrupt pending, at both stepie values.",
        "",
        "cp_prv, cp_prv_at_step and the {stepie, irq_pending} 2x2 cannot be",
        "filled from an M-mode-only program with no interrupt armed.",
        "",
        "  make soc_test_cov CFG_FILE=configs/priv_irq_uvm.json ELF=sw/priv_irq.elf"
    ],
    "scenario":  "priv_irq",
    "transport": "uvm",
    "mode":      "batch",
    "params": { "elf": "sw/priv_irq.elf" },
    "uvm": { "socket_path": "/tmp/uvm_bridge.sock", "timeout": 900.0 }
}
